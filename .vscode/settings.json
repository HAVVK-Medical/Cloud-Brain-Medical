{
  "java.configuration.runtimes": [
    {
      "name": "JavaSE-17",
      "path": "${env:JAVA_HOME}",
      "default": true
    }
  ],
  "java.jdt.ls.java.home": "${env:JAVA_HOME}",
  "java.import.maven.java.home": "${env:JAVA_HOME}",
  "terminal.integrated.env.windows": {
    "JAVA_HOME": "${env:JAVA_HOME}",
    "PATH": "${env:JAVA_HOME}\\bin;${env:PATH}"
  }
}
