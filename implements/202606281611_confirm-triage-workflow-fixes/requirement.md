# Requirement

修复以下已确认问题：
1. 医生端提交处方、结束就诊、患者取消挂号需要确认弹窗，符合文档中的关键操作确认要求。
2. 医生端 AI 工作流侧边栏需要能正确反映失败状态，并在失败时重置/标红，避免卡在运行中。
3. 对话式分诊 SSE 需要更健壮，JSON 解析失败、EventSource 创建失败或连接异常时应有明确降级，不应出现未渲染或卡死。
4. 对话式分诊在 AI 不可用时需要按文档表现出降级提示，并保留用户已输入内容。

约束：不改动无关功能，不新增文档未要求的扩展能力，只修复上述稳定性和交互问题。
